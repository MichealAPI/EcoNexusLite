package it.mikeslab.econexuslite.inventory;

import it.mikeslab.commons.api.inventory.pojo.GuiContext;
import it.mikeslab.econexuslite.EcoNexusLite;
import it.mikeslab.econexuslite.inventory.template.GuiTemplate;

public class DefaultMenu extends GuiTemplate {

    public DefaultMenu(EcoNexusLite instance, GuiContext guiContext) {
        super(instance, guiContext);
    }

}